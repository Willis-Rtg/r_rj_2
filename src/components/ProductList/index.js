import ProdListContainer from "./ProdListContainer";
// import AdminProdListContainer from "./AdminProdListContainer";

export default ProdListContainer;

// export default ({ selectedBrands = [], mode }) => {
//   const props = { selectedBrands, mode };
//   return mode === "USER" ? (
//     <ProdListContainer {...props} />
//   ) : (
//     <AdminProdListContainer {...props} />
//   );
// };
